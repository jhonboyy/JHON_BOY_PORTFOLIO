---
import Menu from './Menu.astro';
import { Image } from 'astro:assets';

const { imageSrc, imageAlt, containerClass, imageStyles, headerClass, worksButtonClass, aboutButtonClass, worksLink, aboutLink, menuImageClass, disableButtonClass1, disableButtonClass2 } = Astro.props;
---

<header class={`menu-opened ${headerClass}`} role="banner">
  <Menu 
  worksButtonClass={worksButtonClass} 
  aboutButtonClass={aboutButtonClass} 
  worksLink={worksLink}
  aboutLink={aboutLink}
  disableButtonClass1={disableButtonClass1}
  disableButtonClass2={disableButtonClass2}

  />
  <div class={`MenuImageContainer ${containerClass}`} style={imageStyles}>
    <Image src={imageSrc} class={menuImageClass} alt={imageAlt} loading="eager" />
  </div>
</header>
